@model IEnumerable<SistemaBandaEdoOaxaca.Genero>

@{
    ViewBag.Title = "Index";
}

<h2>LISTA DE GENEROS</h2>

<p>
    @Html.ActionLink("Crear Genero", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.genero1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.genero1)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idGenero }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.idGenero }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.idGenero })
        </td>
    </tr>
}

</table>
